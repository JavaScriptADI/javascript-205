// loops and arrays
const names = ['anna', 'john', 'nick', 'luka', 'salome', 'andro', 'anano', 'lile' ];
for ( let i = 0; i < names.length ; i++ ) {
    console.log(`hello, ${names[i]}!`);
}
for (let i = names.length -1 ; i >= 0; i--) {
    console.log(names[i]);
}